name: Markdown Check

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  markdown-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: List markdown files
        run: find . -name "*.md" -print
